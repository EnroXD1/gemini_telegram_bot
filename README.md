# Telegram-бот с Gemini, OpenRouter и резервом Groq

**Текущая версия: 1.10.2**

Готовый асинхронный Telegram-бот, который умеет обращаться к Gemini напрямую
или через OpenRouter, а при исчерпании лимита выбранной модели — автоматически
переключаться между всеми настроенными API. Проект рассчитан на личные чаты, группы,
темы форумов и Telegram Business-сообщения. По умолчанию используется long
polling, поэтому домен и HTTPS-сертификат для первого запуска не нужны.

Актуальная конфигурация проекта на 16 августа 2026 года:

- Python 3.11+;
- aiogram 3.30+;
- официальный Google Gen AI SDK 2.11+;
- прямой Google API, OpenRouter или Groq, выбираемый через `.env` или командой
  владельца;
- модели `gemini-3.6-flash` для Google, `google/gemini-3.5-flash` для OpenRouter
  и `llama-3.1-8b-instant` для Groq, а также настраиваемый пул бесплатных
  моделей OpenRouter и Groq;
- SQLite для настроек, ограниченной истории, Business-снимков и минимальной
  статистики использования.
- Pillow 12+ для безопасной локальной нарезки фотографий без обращения к AI.

## Версия 1.10.2

- Фрагменты стенки больше не отправляются альбомами документов: Telegram мог
  визуально переставить файлы внутри медиагруппы, например показать `18` после
  `20`, несмотря на правильные имена.
- Каждый JPEG теперь отправляется отдельным строго последовательным сообщением
  `01 → 24` с подписью номера. Уведомления для этих файлов отключены, чтобы
  большая стенка не создавала серию звуков.
- При ограничении частоты Telegram бот выдерживает указанную сервером паузу и
  повторяет текущий файл, не переходя к следующему раньше времени.

### Версия 1.10.1

- Исправлен запуск `/wall` без аргументов: пустая команда теперь открывает выбор
  сетки и больше не вызывает `IndexError` в обработчике.
- Добавлена отдельная регрессионная проверка пустого, пробельного и числового
  аргумента команды.

### Версия 1.10.0

- Добавлена `/wall` (`/grid`, `/stenka`) — умная нарезка фотографии на профильную
  сетку из 3, 6, 9, 12, 15, 18, 21 или 24 частей.
- Общая область автоматически подгоняется под три колонки. Лёгкий анализ деталей
  немного смещает кадрирование к содержательной части изображения, не ломая
  геометрию и бесшовные границы.
- Каждый фрагмент имеет формат Telegram Story 1080×1920; бесшовная область 4:5
  располагается по центру, а свободные поля заполняются затемнённым размытым фоном.
- Фрагменты получают имена `01…24` и отправляются как файлы уже в обратном порядке
  публикации: достаточно загружать их по номеру, начиная с `01`.
- Нарезка не расходует лимиты Gemini, OpenRouter или Groq. Один фрагмент создаётся
  за раз во временном каталоге; параллельно обрабатывается только одна стенка.
- Добавлены ограничения исходника в 8 МиБ и 12 мегапикселей, защита от анимаций и
  чрезмерной распаковки, а также настраиваемая ширина Story 1080 px.

### Версия 1.9.4

- Обработка вложений адаптирована к фактическому лимиту контейнера Bothost: полный цикл загрузки и
  распознавания медиа выполняется по одному, а сырые байты освобождаются до начала следующего
  медиазапроса. Безопасный лимит одного запроса по умолчанию снижен до 8 МиБ.
- Google SDK больше не создаёт скрытую серию повторов поверх повторов самого бота. Ошибки квоты
  `429` сразу запускают резервный маршрут вместо многократной отправки одного файла.
- Для Google создаётся только то представление вложения, которое действительно использует
  доступный API. При переключении провайдера бот освобождает traceback и временные буферы до
  кодирования файла для следующей модели.
- Медиа для OpenRouter кодируется в Base64 потоком по 64 КиБ и отправляется с заранее вычисленной
  длиной тела. Цельная Base64-строка и её вторая JSON-копия больше не создаются в памяти.
- В журнал добавлены провайдер, модель, число вложений и их общий размер для каждой попытки. Это
  позволяет отличить долгий ответ API от остановки процесса по памяти.
- Динамический `openrouter/free` сохранён в мультимодальном резерве: OpenRouter самостоятельно
  фильтрует бесплатный пул по необходимой модальности запроса.

### Версия 1.9.3

- Обычные ответы, объясняющие термины `request blocked`, `content filter` или
  `policy violation`, больше не принимаются за служебную блокировку модели.
- Распознаются дополнительные причины блокировки Google, включая `BLOCKLIST`,
  `PROHIBITED_CONTENT`, `SPII`, `IMAGE_SAFETY` и ограничения цитирования.
- Частичный текст ответа с причиной блокировки не отправляется пользователю:
  бот автоматически переходит к следующей доступной модели.

### Версия 1.9.2

- Safety-блокировки и служебные ответы OpenRouter/Groq вида `request blocked`,
  `content_filter` или `User Safety / Response Safety` теперь считаются
  причиной для автоматического переключения на следующую доступную модель.
- Если динамический `openrouter/free` выбирает не чат-модель, например
  `content-safety`, бот не отправляет её отчёт пользователю, а пробует Groq,
  Google или следующий доступный маршрут.
- Индикатор обработки показывает отдельную причину переключения: модель не
  смогла обработать конкретный запрос, поэтому бот пробует резерв.

### Версия 1.9.1

- Варианты одного эмодзи с невидимым Unicode-модификатором (`⚡️`) и без него
  (`⚡`) теперь считаются одним символом. Уже сохранённые соответствия
  автоматически распознаются, поэтому повторно вводить `/emoji set` не нужно.
- Добавлена команда `/emoji test ⚡` для визуальной проверки выбранного
  кастомного эмодзи непосредственно в Telegram.

### Версия 1.9.0

- Сообщение, состоящее только из кастомного эмодзи, теперь молча игнорируется в
  группах и не попадает ни в настройщик эмодзи владельца, ни в AI-обработку при
  режиме `/mode all`. Текстовые сообщения с эмодзи продолжают работать обычно.
- В автоматический резервный пул OpenRouter добавлены бесплатные
  `openai/gpt-oss-20b:free`, `nvidia/nemotron-3.5-lightning:free` и динамический
  `openrouter/free`. Ошибка оплаты основной модели больше не блокирует попытку
  перейти на бесплатные модели того же API.
- В пул Groq добавлены `openai/gpt-oss-20b` и `llama-3.3-70b-versatile`, имеющие
  бесплатные лимиты GroqCloud. Списки можно переопределить или отключить через
  `OPENROUTER_FALLBACK_MODELS` и `GROQ_FALLBACK_MODELS`.
- Команда `/model` теперь показывает всю фактическую цепочку автоматического
  резерва, а не только основные модели настроенных провайдеров.

### Версия 1.8.8

- Удалена случайная палитра эмодзи: `/start`, `/guide`, уведомления и статусы
  теперь всегда используют свой предусмотренный символ или явно назначенную
  владельцем замену через `/emoji`.
- Отправленный боту кастомный эмодзи или набор больше не меняет оформление:
  бот только показывает найденные `custom_emoji_id` и пример команды настройки.

### Версия 1.8.7

### Версия 1.8.6

### Версия 1.8.5

### Версия 1.8.4

### Версия 1.8.3

### Версия 1.8.2

- Ответ владельца запускает сохранение только для медиа, которое Telegram явно
  пометил `has_protected_content`: обычные фото, видео и кружки больше не
  дублируются в личный чат бота при ответе на них.
- Визуальный спойлер `has_media_spoiler` не считается таймером самоуничтожения и
  также не запускает сохранение. При отсутствии явного признака бот безопасно
  игнорирует ответ, чтобы не создавать ложные копии.
- Добавлены отдельные регрессионные проверки для обычных фото, видео, кружков и
  медиа под спойлером.

### Версия 1.8.1

- Таймерное фото или видео теперь можно сохранить явным действием: владелец
  отвечает на него любым сообщением в Business-диалоге до открытия. Бот извлекает
  вложение из `reply_to_message` и сразу отправляет сохранённую копию в личный чат
  владельца с ботом.
- Запросом на сохранение считается только ручной ответ владельца. Исходящие
  сообщения самого Business-бота игнорируются, поэтому AI-ответ не может случайно
  запустить сохранение или цикл сообщений.
- Повторный ответ на то же медиа не создаёт новую копию, а последующее событие
  удаления не дублирует уже доставленный файл.
- Удаления неизвестных Business-сообщений больше не создают уведомления вида
  «автор и исходная версия неизвестны». Они остаются только в техническом журнале,
  что исключает каскад сообщений из-за служебных и исходящих удалений Telegram.

### Версия 1.8.0

- При `402/429` выбранной модели бот автоматически пробует остальные настроенные
  модели и провайдеры: Google, OpenRouter и Groq. Ручной выбор `/model` остаётся
  основной моделью и не перезаписывается временным переключением.
- Пользователь текущего диалога видит, у какой модели закончился лимит и на какую
  модель выполняется переход. При нескольких отказах статус обновляется для
  каждого следующего шага цепочки.
- Исчерпавшая квоту модель ставится на десятиминутную паузу, чтобы новые запросы
  не тратили время и квоту на заведомо недоступный маршрут.
- Ограниченная SQLite-история теперь поддерживается при любом основном
  провайдере, поэтому контекст не теряется при автоматическом переходе между API.
- Антиспам действует глобально по Telegram ID, а не отдельно для каждого чата.
  Один пользователь не может поставить новый AI-запрос, пока обрабатывается его
  предыдущий запрос.
- После трёх попыток обойти ограничение частоты AI-доступ пользователя временно
  блокируется на пять минут. Владельцы из `OWNER_IDS` от этих лимитов освобождены.

### Версия 1.7.0

- Business-медиа теперь скачивается в тихий локальный архив: бот не присылает
  владельцу дубликат каждого нового фото, голосового или кружка.
- Сохранённый файл отправляется владельцу только после того, как собеседник
  удалит исходное сообщение. После успешной отправки локальная копия удаляется.
- Снимок сообщения и файл связаны через SQLite, поэтому несколько кружков не
  перепутаются. Повторное событие удаления не создаёт повторную копию.
- Скачивание и обработка удаления синхронизированы для каждого чата: даже очень
  быстро удалённое медиа не обгонит сохранение файла.
- Невостребованные архивы автоматически очищаются вместе со снимками сообщений
  по `BUSINESS_MESSAGE_RETENTION_DAYS` и сохраняются в том же volume, что SQLite.

### Версия 1.6.1

- Исправлено удаление временного индикатора в Telegram Business: бот использует
  специальный метод `deleteBusinessMessages`, если обычное удаление недоступно.
- Запасное сообщение «✅ Обработка завершена» больше не остаётся в переписке:
  после временной ошибки Telegram выполняется повторная фоновая попытка удаления.
- Основной ответ не задерживается на время повторной очистки индикатора.

### Версия 1.6.0

- Бот теперь проверяет статус Interactions API и `finish_reason` в ответах
  Google/Gemini, OpenRouter и Groq. Если модель остановилась из-за лимита
  токенов, продолжение запрашивается автоматически и объединяется с уже
  полученным текстом.
- Продолжение получает предыдущую часть как ответ ассистента и отдельную строгую
  инструкцию не повторять решение. Это особенно важно для динамического роутера
  `openrouter/free`, который может выбирать разные бесплатные модели.
- По умолчанию выполняется не более двух продолжений — максимум три AI-запроса
  на один пользовательский запрос. Лимит настраивается переменной
  `AI_MAX_CONTINUATIONS` в диапазоне от `0` до `5`.
- Если все продолжения снова упёрлись в лимит или оборвались по сети, бот
  сохраняет полученный текст, явно предупреждает пользователя и отправляет его
  без Markdown-обработки, чтобы незакрытая разметка не ломала сообщение.
- В журнале теперь фиксируются фактическая модель, `finish_reason` и номер части,
  что упрощает диагностику нестабильных бесплатных моделей.

### Версия 1.5.0

- Добавлена владельческая команда `/model`: она переключает основной провайдер
  между Google, OpenRouter и Groq и позволяет указать точный ID модели.
- Используются только провайдеры, для которых настроены API-ключи. Команда
  защищена числовым Telegram ID из `OWNER_IDS`.
- Выбранные провайдер и модель сохраняются в SQLite и автоматически
  восстанавливаются после перезапуска или повторного развёртывания.
- Groq теперь может работать не только как резерв OpenRouter, но и как основной
  текстовый провайдер. При смене модели контексты сбрасываются, чтобы ответы
  разных API не смешивались.
- Сообщение об исчерпанном лимите удаляется через 20 секунд и при неудачном
  переключении OpenRouter → Groq, а также при прямой ошибке лимита Groq.
- Все настроенные API-клиенты создаются при запуске, поэтому переключение не
  требует правки переменных окружения или рестарта контейнера.

### Версия 1.4.0

- Добавлен необязательный бесплатный резерв Groq: текстовый запрос автоматически
  переключается на него после окончательной ошибки `402`, `429` или `5xx` от
  OpenRouter.
- Во время переключения пользователь видит временное сообщение
  «Переключаюсь на резервную модель Groq»; после ответа оно удаляется.
- Ошибка об исчерпанном лимите OpenRouter не отправляется, если резерв ответил.
  Если резерв не настроен, сообщение об ошибке `429` удаляется через 20 секунд.
- Локальная история OpenRouter передаётся резерву, поэтому диалог не начинается
  с нуля. Двоичные фото, аудио, видео и PDF в текстовый резерв не отправляются.
- `/status` показывает готовность резерва, а `/stats` и `/users` отдельно
  учитывают успешные ответы Groq.
- Существующая SQLite-база автоматически получает новый счётчик Groq без потери
  сохранённых данных.

### Версия 1.3.1

- Закрыта ошибка авторизации `/users` и `/stats`: глобальные отчёты теперь
  доступны исключительно Telegram ID из `OWNER_IDS`.
- Подключение Telegram Business больше не выдаёт аккаунту права на глобальную
  статистику и уведомления о пользователях самого бота.
- Business-владелец по-прежнему может управлять только собственными командами
  `/autoreply` и `/chats`.
- Username не используется как доказательство владения: Telegram ID остаётся
  стабильным даже после переименования аккаунта.

### Версия 1.3.0

- Добавлены `/users` и `/stats` для владельца: список недавних пользователей,
  активность, число обращений и фактических AI-запросов по провайдерам.
- При первом обращении нового пользователя владелец получает уведомление с
  именем, username, Telegram ID и источником обращения.
- `/chats` управляет автоответами для отдельных Business-собеседников, а
  `/autoreply off` включает общий режим «только мониторинг».
- `/status` показывает фактически запущенную версию бота.
- Версии в `bot/__init__.py`, `pyproject.toml` и этом README проверяются тестом,
  чтобы документация не расходилась с кодом.

## Что уже реализовано

- Текст, подписи к медиа и обычные ответы на сообщения.
- Фото, статические и видео-стикеры, GIF/анимации, видео и видеосообщения.
- Аудиофайлы и голосовые сообщения: Gemini может распознать речь, сделать
  пересказ или ответить по содержимому.
- PDF и текстовые файлы (`txt`, `csv`, `json`, `xml`, исходный код и подобные).
- Геолокации, места, контакты, опросы, dice, игры, чек-листы и rich messages в
  виде структурированного текстового контекста.
- Telegram-альбом передаётся Gemini как один запрос, а не как несколько
  несвязанных сообщений.
- При reply бот передаёт текст и доступное вложение исходного сообщения вместе
  с новым вопросом.
- Отдельный контекст для личного чата, группы/темы или каждого пользователя —
  настраивается переменной `CONVERSATION_SCOPE`.
- Последовательная обработка одного диалога: два одновременных сообщения не
  перепутают цепочку запросов.
- Глобальное по Telegram ID ограничение частоты, защита от очереди запросов одного
  пользователя, временная блокировка спамеров, лимиты файлов и общего параллелизма.
- Повтор временных ошибок Gemini, понятные сообщения для 400/401/403/429/5xx и
  автоматический новый контекст, если сохранённая цепочка уже истекла.
- Автоматический переход на `generateContent`, если Interactions API недоступен
  в регионе хостинга.
- Обычный Markdown модели безопасно превращается в Telegram-оформление:
  заголовки, жирный и курсивный текст, ссылки, цитаты, списки и блоки кода.
- Ответы длиннее Telegram-лимита делятся с сохранением форматирования и точных
  UTF-16-смещений Telegram entities.
- На время запроса показывается анимированный статус обработки, который затем
  автоматически удаляется; стандартный индикатор «печатает…» также остаётся.
- При лимите любой выбранной модели временный статус показывает переход на
  следующую доступную модель и удаляется после готового ответа.
- Команды администратора группы и allowlist для закрытого бота.
- Учёт пользователей самого бота без сохранения текста их сообщений: имя,
  username, ID, время активности, источник и счётчики AI-запросов.
- Уведомление владельцу при первом обращении нового пользователя, а также
  владельческие отчёты `/users` и `/stats`.
- Локальная нарезка фотографии на бесшовную профильную стенку из 3–24 частей
  через `/wall`; API нейросетей для этого не используются.
- Мгновенные уведомления владельцу Telegram Business-аккаунта, если собеседник
  изменил или удалил сообщение; в уведомлении остаётся исходный текст.
- Одноразовое приветствие каждому Business-собеседнику перед первым AI-ответом:
  оно сообщает об автоответчике и доступных командах.
- Автоматическое сохранение входящих фото, видео, GIF, аудио, голосовых,
  кружков и документов: файл скачивается сразу и без уведомления, а владельцу
  отправляется только после удаления исходного сообщения.
- Секреты не записываются в исходники, SQLite и логи.

Важно: обычный Telegram-бот видит только личный чат с самим ботом и те
группы/каналы, куда его добавили. Он не получает произвольные личные переписки
вашего аккаунта. Для автоматических ответов в диалогах бизнес-аккаунта подключите
его как Telegram Business bot с правом чтения сообщений. Обработчики новых,
изменённых и удалённых Business-сообщений включены, а контекст каждой business
connection отделён от обычного чата.

## Стенка из фотографий

Функция работает в личном чате и не использует нейросеть:

1. Отправьте фотографию с подписью `/wall` или ответьте `/wall` на уже
   отправленное изображение.
2. Выберите 3, 6, 9 … 24 части. Можно сразу указать число: `/wall 9`.
3. Бот пришлёт превью и вертикальные JPEG-файлы 1080×1920 с номерами. Загружайте их строго
   от `01` к последнему номеру — обратный порядок нужен, чтобы новые элементы
   профиля сложились в исходную картинку.

Поддерживаются статичные JPEG, PNG и WEBP. Изображение автоматически
ориентируется по EXIF и кадрируется под сетку из трёх колонок. Исходный файл
ограничен 8 МиБ и 12 мегапикселями. В `.env` лимиты можно уменьшить; верхняя граница
12 Мп намеренно зафиксирована для Bothost с небольшим контейнером.

## Защита Telegram Business-переписки

1. В Telegram откройте **Настройки → Telegram Business → Чат-боты**.
2. Подключите созданного через BotFather бота и разрешите ему читать сообщения.
3. Откройте личный чат с ботом и нажмите **Start**, чтобы он мог присылать вам
   уведомления и сохранённые файлы.
4. Оставьте `BUSINESS_MONITOR_ENABLED=true` и `BUSINESS_ARCHIVE_MEDIA=true`.

Чтобы оставить только защитные функции без ответов собеседникам, отправьте боту
в личном чате `/autoreply off`. Команда отключает для владельца Business-аккаунта
ИИ-ответы, ответы на команды и приветствие, но не затрагивает снимки сообщений,
тихий архив медиа и уведомления об изменениях/удалениях. `/autoreply on` включает
ответы обратно, а `/autoreply status` показывает текущий режим. Выбор хранится в
SQLite и переживает перезапуск бота.

Для отдельных собеседников используйте `/chats` в личном чате с ботом. Бот
покажет последние 20 Business-диалогов с inline-кнопками: `✅` означает, что
автоответы включены, а `🔕` — что для этого собеседника работает только
мониторинг. Команды в переписку с собеседником отправлять не нужно. Общий
`/autoreply off` имеет приоритет и временно отключает ответы во всех чатах.

При новом входящем Business-сообщении бот сохраняет текстовый снимок в SQLite.
При `edited_business_message` он присылает версии «было/стало», а при
`deleted_business_messages` — удалённый текст. Поддерживаемое медиа скачивается
немедленно, но хранится без сообщений в локальном архиве. В личный чат владельца
оно загружается только после удаления оригинала, поэтому новые кружки и другие
файлы не дублируются. После успешной отправки архивная копия удаляется.

Telegram Bot API не сообщает значение таймера самоуничтожения для Business-медиа,
но помечает недоступное для сохранения сообщение признаком
`has_protected_content`. Чтобы явно сохранить такое таймерное фото или видео,
ответьте на него любым сообщением в диалоге с собеседником **до открытия**.
Вложение из ответа будет загружено в личный чат владельца с ботом. Обычные фото,
видео, кружки и медиа под визуальным спойлером при ответе игнорируются; ответы
самого бота и повторные ответы на уже сохранённое медиа тоже не создают копий.
Если Telegram не передал явный признак защищённого содержимого, бот не пытается
угадывать тип медиа и не создаёт дубликат. Обычный Bot API не имеет доступа к
секретным чатам; таймерные файлы из секретного чата сохранить таким способом
нельзя. Размер одной сохраняемой копии ограничен `BUSINESS_ARCHIVE_MAX_BYTES`, а
текстовые снимки и невостребованные файлы автоматически удаляются по истечении
`BUSINESS_MESSAGE_RETENTION_DAYS`.

Неподдерживаемые бинарные форматы (например, архив или исполняемый файл) не
отправляются Gemini. Бот передаёт модели только имя и MIME-тип и сообщает, что
содержимое недоступно. Анимированные TGS-стикеры также описываются текстом;
статические WebP и видео-стикеры WebM анализируются полностью.

## Быстрый запуск без Docker

1. Создайте бота через [@BotFather](https://t.me/BotFather), скопируйте токен.
2. Создайте ключ в [Google AI Studio](https://aistudio.google.com/app/apikey)
   либо в [OpenRouter](https://openrouter.ai/keys).
3. Установите Python 3.11 или новее и выполните:

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
pip install -r requirements.txt
cp .env.example .env
```

4. Откройте `.env` и выберите один из вариантов. Для прямого Google API:

```dotenv
TELEGRAM_BOT_TOKEN=токен_от_BotFather
GEMINI_API_KEY=ключ_из_Google_AI_Studio
```

Для OpenRouter:

```dotenv
TELEGRAM_BOT_TOKEN=токен_от_BotFather
AI_PROVIDER=openrouter
OPENROUTER_API_KEY=ключ_из_OpenRouter
OPENROUTER_MODEL=google/gemini-3.5-flash
```

5. Запустите:

```bash
python -m bot
```

Альтернативный запуск после `pip install .`:

```bash
gemini-telegram-bot
```

## Запуск через Docker Compose

```bash
cp .env.example .env
# заполните TELEGRAM_BOT_TOKEN и ключ выбранного AI-провайдера
docker compose up -d --build
docker compose logs -f bot
```

База сохраняется в Docker volume `bot_data`. Контейнер работает от отдельного
непривилегированного пользователя, с read-only файловой системой и
`no-new-privileges`.

## Развёртывание на bothost.ru

1. Отправьте проект в GitHub/GitLab и на странице создания бота Bothost укажите
   URL репозитория, ветку `main`, платформу Telegram и главный файл `main.py`.
2. Используйте стандартную Python-сборку Bothost (`requirements.txt` уже есть).
   Собственный `Dockerfile` также поддерживается, но для этого проекта не нужен.
3. Для OpenRouter добавьте `AI_PROVIDER=openrouter`, `OPENROUTER_API_KEY` и
   `OPENROUTER_MODEL=google/gemini-3.5-flash`. Для прямого Google API добавьте
   `GEMINI_API_KEY`; для ключа Vertex AI Express Mode (например, с префиксом
   `AQ.`) также установите `GEMINI_VERTEX_AI=true`. Если токен из формы
   создания не передаётся в контейнер, добавьте его как
   `APP_TELEGRAM_BOT_TOKEN`. Также поддерживаются `TELEGRAM_BOT_TOKEN` и
   системные алиасы `BOT_TOKEN`, `BOT_API_TOKEN`, `TOKEN`.
4. Установите `DATABASE_PATH=/app/data/bot.sqlite3`, чтобы снимки сообщений и
   контекст сохранялись между обновлениями.
5. После запуска проверьте runtime-логи, запустите бота в Telegram и подключите
   его как Business bot по инструкции выше.

Не загружайте `.env` в репозиторий: токены и API-ключи задаются только через
переменные окружения Bothost.

## Как работает маршрут через OpenRouter

Бот сначала получает Telegram update и скачивает поддерживаемое вложение в
оперативную память процесса. Затем он собирает единый запрос: текст остаётся
текстом, изображения и видео превращаются в Base64 data URL, аудио — в raw
Base64 с указанием формата, PDF — в Base64-файл. Запрос отправляется по HTTPS на
`https://openrouter.ai/api/v1/chat/completions` с ключом OpenRouter.

OpenRouter проверяет ключ и баланс, находит доступного поставщика выбранной
модели и передаёт ему запрос. Полученный текстовый ответ возвращается тем же
маршрутом в бот, после чего бот делит длинный текст по лимиту Telegram. VPN
пользователя в этой цепочке не участвует: сетевое соединение выполняет сервер
Bothost.

При `AI_PROVIDER=openrouter` ключ и оплата принадлежат OpenRouter, поэтому Google
Cloud-проект и его выбор страны не нужны. Содержимое запроса при этом видят как
OpenRouter, так и фактический поставщик модели. Для продолжения диалога бот
хранит в SQLite ограниченную текстовую историю и прикладывает её к следующему
запросу. По умолчанию это не более 6 обменов и 16 000 символов; содержимое
прошлых файлов повторно не сохраняется и не загружается. `/reset` удаляет
локальную историю текущего диалога. Текст и вложение сообщения, на которое
сделан Telegram reply, также входят в текущий запрос.

## Поведение в группах

Доступны три режима:

| Режим | Поведение |
| --- | --- |
| `mentions` | Ответ на `/ask`, `@username` бота или reply на сообщение бота |
| `all` | Ответ на каждое поддерживаемое сообщение |
| `off` | Автоматические ответы отключены, но `/ask` работает |

По умолчанию включён безопасный режим `mentions`. Администратор меняет режим
командой `/mode mentions`, `/mode all` или `/mode off`.

Чтобы режим `all` действительно видел обычные сообщения группы, отключите для
бота Group Privacy через `@BotFather` → `/mybots` → Bot Settings → Group Privacy.
Telegram с включённой приватностью отдаёт боту только команды, упоминания и
ответы, направленные ему. Telegram также не передаёт обычным ботам сообщения
других ботов, что защищает от бесконечных циклов.

В forum-группах каждая тема автоматически получает отдельный контекст. При
`CONVERSATION_SCOPE=chat_thread_user` контекст дополнительно разделяется по
авторам.

## Команды

| Команда | Назначение |
| --- | --- |
| `/start` | Краткое знакомство |
| `/help` | Возможности и режимы |
| `/wall [3–24]` | Нарезать фотографию на бесшовную профильную стенку |
| `/ask текст` | Явный запрос; поддерживается и в подписи файла |
| `/reset` | Разорвать текущую цепочку контекста |
| `/cancel` | Остановить выполняющийся запрос |
| `/autoreply on\|off\|status` | Автоответы Telegram Business; только владелец |
| `/chats` | Индивидуальные режимы Business-собеседников |
| `/users [1–15]` | Последние пользователи самого бота; только ID из `OWNER_IDS` |
| `/stats` | Сводная статистика и расход AI-запросов; только ID из `OWNER_IDS` |
| `/model [provider] [model]` | Показать или сменить AI-провайдер/модель; только ID из `OWNER_IDS` |
| `/status` | Показать модель, режим и наличие контекста |
| `/mode ...` | Настроить группу; только администратор |

В группе `/reset` и `/cancel` доступны администраторам, потому что стандартный
контекст является общим для всей темы.

## Основные настройки `.env`

| Переменная | Значение по умолчанию | Назначение |
| --- | ---: | --- |
| `AI_PROVIDER` | `google` | `google`, `openrouter` или `groq` |
| `GEMINI_MODEL` | `gemini-3.6-flash` | ID модели Gemini |
| `GEMINI_VERTEX_AI` | `false` | Использовать Vertex AI Express Mode |
| `OPENROUTER_MODEL` | `google/gemini-3.5-flash` | Модель через OpenRouter |
| `OPENROUTER_FALLBACK_MODELS` | три бесплатных маршрута | Резервный пул OpenRouter через запятую |
| `OPENROUTER_HISTORY_TURNS` | `6` | Число локальных обменов в контексте |
| `OPENROUTER_HISTORY_MAX_CHARS` | `16000` | Общий лимит передаваемой истории |
| `OPENROUTER_HISTORY_RETENTION_DAYS` | `30` | Срок неактивной локальной истории |
| `GROQ_API_KEY` | пусто | Ключ бесплатного текстового резерва Groq |
| `GROQ_FALLBACK_ENABLED` | зависит от ключа | Разрешить Groq участвовать в резервной цепочке |
| `GROQ_MODEL` | `llama-3.1-8b-instant` | Резервная текстовая модель Groq |
| `GROQ_FALLBACK_MODELS` | `gpt-oss-20b`, `llama-3.3-70b` | Дополнительный бесплатный пул Groq |
| `GROQ_MAX_OUTPUT_TOKENS` | `1024` | Лимит ответа резерва, экономящий бесплатную квоту |
| `GEMINI_SYSTEM_PROMPT` | встроенный русский prompt | Роль и стиль бота |
| `GEMINI_TEMPERATURE` | `0.7` | Вариативность ответа, от 0 до 2 |
| `GEMINI_MAX_OUTPUT_TOKENS` | `4096` | Максимум токенов ответа |
| `AI_MAX_CONTINUATIONS` | `2` | Автопродолжения ответа при достижении лимита токенов, от 0 до 5 |
| `AI_AUTO_FALLBACK_ENABLED` | `true` | Автопереход между всеми настроенными AI API |
| `AI_FALLBACK_COOLDOWN_SECONDS` | `600` | Пауза модели после исчерпания квоты |
| `GEMINI_STORE_INTERACTIONS` | `true` | Серверный многоходовый контекст |
| `GROUP_DEFAULT_MODE` | `mentions` | Начальный режим новой группы |
| `CONVERSATION_SCOPE` | `chat_thread` | Общий или пользовательский контекст |
| `OWNER_IDS` | пусто | Числовые Telegram ID владельцев через запятую; защищают `/users` и `/stats` |
| `ALLOWED_CHAT_IDS` | пусто | Разрешённые chat ID; пусто означает все |
| `BUSINESS_MONITOR_ENABLED` | `true` | Снимки и уведомления Business-сообщений |
| `BUSINESS_AUTO_REPLY_ENABLED` | `true` | Начальное состояние Business-автоответов |
| `BUSINESS_ARCHIVE_MEDIA` | `true` | Тихий архив, отправка при удалении и сохранение через ответ владельца |
| `BUSINESS_ARCHIVE_MAX_BYTES` | `20971520` | Лимит одной сохраняемой копии, 20 МиБ |
| `BUSINESS_MESSAGE_RETENTION_DAYS` | `30` | Срок хранения снимков и тихого медиаархива |
| `BUSINESS_WELCOME_ENABLED` | `true` | Приветствие новому Business-собеседнику |
| `MAX_MEDIA_BYTES` | `8388608` | Общий лимит медиа одного запроса, 8 МиБ |
| `MAX_MEDIA_ITEMS` | `10` | Максимум файлов/кадров в запросе |
| `WALL_MAX_SOURCE_BYTES` | `8388608` | Максимальный размер исходника `/wall`, 8 МиБ |
| `WALL_MAX_PIXELS` | `12000000` | Максимальное разрешение исходника, 12 Мп |
| `WALL_TILE_SIZE` | `1080` | Ширина Story-фрагмента 9:16, 128–1440 px |
| `MAX_CONCURRENT_REQUESTS` | `4` | Глобальный параллелизм AI-запросов |
| `RATE_LIMIT_REQUESTS` | `8` | Запросов пользователя за окно |
| `RATE_LIMIT_WINDOW_SECONDS` | `60` | Размер rate-limit окна |
| `SPAM_VIOLATION_LIMIT` | `3` | Нарушений rate limit до временной блокировки |
| `SPAM_BLOCK_SECONDS` | `300` | Длительность блокировки AI-запросов спамера |
| `DROP_PENDING_UPDATES` | `false` | Удалять ли накопленные Telegram updates при старте |

Все доступные параметры и комментарии находятся в `.env.example`.

Без аргументов `/model` показывает доступные API. `/model groq`,
`/model openrouter` и `/model google` выбирают стандартную модель соответствующего
провайдера; точный ID задаётся вторым аргументом, например
`/model openrouter google/gemini-2.5-flash`. `/model default` возвращает значения
из окружения. Переключение глобально для всех чатов и сохраняется в SQLite.

Для авторизации владельца указывайте именно числовой Telegram ID, а не
`@username`: username пользователь может изменить, тогда как ID аккаунта
остаётся прежним. Если `OWNER_IDS` пуст, глобальные отчёты `/users` и `/stats`
не доступны никому. Само по себе подключение бота в разделе Telegram Business
не предоставляет доступ к этим отчётам.

## Контекст и приватность

Последние обмены при любом основном провайдере хранятся локально в SQLite с
жёсткими лимитами по числу и объёму, чтобы автоматическое переключение модели не
обрывало контекст. В Google-режиме дополнительно может использоваться серверная
цепочка Interactions API. Неактивная локальная история удаляется через 30 дней. Для
защиты Business-переписки SQLite дополнительно хранит исходный текст, имя
отправителя и тип вложения до 30 дней; сами файлы в SQLite не записываются, а
отправляются владельцу в Telegram. Токены и ключи в базу не записываются.
`/reset` удаляет локальную OpenRouter-историю или перестаёт использовать прежнюю
Google-цепочку, но не удаляет серверные журналы Google и снимки Business-сообщений.

Если настроено несколько API-ключей, при лимите или временной недоступности
выбранной модели бот по очереди пробует другие настроенные модели. Текст,
системная инструкция и ограниченная локальная история передаются новому
провайдеру; двоичные вложения не передаются текстовой модели Groq. Всю ротацию
можно отключить через `AI_AUTO_FALLBACK_ENABLED=false`, а участие Groq как
резерва — через `GROQ_FALLBACK_ENABLED=false`. Актуальные бесплатные лимиты
опубликованы в [официальной документации Groq](https://console.groq.com/docs/rate-limits).

Для отчётов `/users` и `/stats` SQLite хранит отдельную минимальную запись на
каждого пользователя самого бота: Telegram ID, имя, username, время первого и
последнего обращения, последний тип чата и числовые счётчики. Текст сообщений и
вложения в таблицу статистики не записываются. Собеседники подключённого
Telegram Business-аккаунта в эту статистику не входят и отображаются отдельно
через `/chats`. Такой учёт не создаёт дополнительных запросов к OpenRouter,
Groq или Google.

По актуальной документации Gemini срок хранения Interaction по умолчанию — до
55 дней на платном уровне и 1 день на бесплатном. Настройки хранения и ручное
удаление доступны в Google AI Studio. Если требуется одноразовая обработка без
серверной истории, установите:

```dotenv
GEMINI_STORE_INTERACTIONS=false
```

В этом режиме каждое обращение независимо; reply-контекст текущего Telegram-
сообщения всё равно передаётся. Любое анализируемое содержимое отправляется
выбранному AI-провайдеру. В режиме OpenRouter оно проходит через OpenRouter к
фактическому поставщику модели. Перед добавлением бота в рабочий чат следует
уведомить участников и проверить применимые правила обработки данных.

Храните `.env` только на сервере. Если токен или ключ попал в публичный репозиторий,
немедленно перевыпустите его в BotFather/AI Studio, а не просто удаляйте строку из
последнего коммита.

## Ограничения и масштабирование

- Проект использует long polling. Telegram не разрешает одновременно получать
  updates через webhook и polling; при старте код удаляет старую webhook-
  конфигурацию.
- SQLite и локальные блокировки рассчитаны на один экземпляр процесса. Для
  нескольких реплик потребуется общий PostgreSQL/Redis, распределённые блокировки
  и webhook-доставка с идемпотентностью updates.
- Максимальный размер намеренно установлен ниже лимитов Gemini и Telegram, чтобы
  base64-запросы не занимали слишком много памяти. Увеличивайте его только вместе
  с лимитами RAM и таймаутами.
- Модели имеют жизненный цикл. При прекращении поддержки достаточно заменить
  `GEMINI_MODEL` или `OPENROUTER_MODEL`, не меняя обработчики Telegram.

## Проверка проекта

Полная локальная проверка не обращается к Telegram, Gemini или OpenRouter:

```bash
pytest -q
python -m compileall -q bot tests main.py
ruff check .
```

Перед проверкой установите dev-группу, если она ещё не установлена:

```bash
pip install -e '.[dev]'
```

Реальный end-to-end тест требует собственных ключей: отправьте боту текст,
фото с подписью, reply на фото, голосовое, PDF и альбом; затем проверьте режимы
группы и `/reset`.

## Официальные источники

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram Bots FAQ и Group Privacy](https://core.telegram.org/bots/faq)
- [aiogram 3 documentation](https://docs.aiogram.dev/)
- [Gemini Interactions API](https://ai.google.dev/gemini-api/docs/interactions-overview)
- [Gemini multimodal file inputs](https://ai.google.dev/gemini-api/docs/file-input-methods)
- [Google Gen AI Python SDK](https://googleapis.github.io/python-genai/)
- [OpenRouter Chat Completions](https://openrouter.ai/docs/api/api-reference/chat/send-chat-completion-request)
- [OpenRouter multimodal inputs](https://openrouter.ai/docs/guides/overview/multimodal/overview)
